# 核心流程验收清单

> 审查日期：2026-08-18
>
> 状态说明：✅ 已通过；⚠️ 主流程可用但仍需补强；❌ 发布前阻塞项。

## 总体结论

CLI 的核心流程已经基本完整，Library、Agent Activation、项目部署、项目 Skill 迁移、
schema 迁移和发布安装链路均有实现及自动化覆盖。Web UI 仍有三项发布前阻塞问题：
写接口缺少跨站请求防护、Git Source 更新后不会同步已部署 Skill、空白项目无法首次部署。

## 核心流程验收

| 流程 | 状态 | 当前结论 | 后续完善 |
| --- | --- | --- | --- |
| Library 初始化 | ✅ | `skm init` 主流程和隔离测试已覆盖 | 保持回归测试 |
| 本地 Skill 添加、校验、查看、删除 | ✅ | CLI 闭环完整 | 保持回归测试 |
| 标签管理与按标签批量启用 | ✅ | 增删、筛选和批量启用可用 | 增补 Web 侧覆盖 |
| Prompt 本地管理与变量渲染 | ✅ | CLI 与 Web 支持创建、导入、校验、更新、渲染、导出和删除 | 后续增加 Git Source 与历史版本 |
| 用户级 Agent Activation | ✅ | 可扫描本机 Agent 目录并自定义管理范围；启用、禁用和状态检查可用 | 补充更多 Agent 的真实目录验收 |
| 幂等部署与冲突保护 | ✅ | 不覆盖未知目标，可识别受管链接或副本 | 增补故障注入测试 |
| Git Source 添加与更新 | ⚠️ | CLI 闭环完整；Web 更新只刷新 Library | Web 更新后同步已部署版本 |
| Git Source 删除 | ⚠️ | CLI 可用 | Web 增加删除入口和依赖提示 |
| 已登记项目的 link、copy、unlink | ⚠️ | CLI 可用；已有 Agent 目录的 Web 流程可用 | 修复空白项目首次部署 |
| 项目扫描与详情展示 | ✅ | 可发现项目 Agent 和 Skill 状态 | 增补浏览器端回归测试 |
| 项目 Skill follow、copy、move 与 fallback | ✅ | 主迁移路径已实现 | Doctor 应区分正常 fallback 与损坏状态 |
| Project Require 恢复 | ⚠️ | 实现和单元/集成测试存在 | 增加“干净 HOME + 真实 Git 仓库”的完整端到端测试 |
| Project Vendor | ⚠️ | 基础复制流程可用 | 覆盖编辑、重新应用和失败恢复 |
| schema v1 到 v2 迁移 | ✅ | 自动迁移及兼容行为已覆盖 | 保留旧样本回归测试 |
| Doctor 自检 | ⚠️ | Claude Code、Codex 的主要检查可用 | 覆盖所有已配置 Agent，并细分项目状态 |
| Web 写接口安全 | ❌ | 仅设置响应安全头，未验证 Origin、Host、Content-Type 或 CSRF token | 发布前补齐请求校验 |
| Web 浏览器端到端测试 | ❌ | 当前没有可执行的真实浏览器验收链路 | 覆盖 Library、Source、Activation、Project 主流程 |
| Pull Request 持续集成 | ❌ | 发布工作流主要由版本 Tag 触发 | 增加 push/PR 的 test、vet、gofmt 和安装检查 |
| Release 安装链路 | ⚠️ | 安装器和 Homebrew Formula 检查通过 | macOS 增加签名与公证 |

## 发布前阻塞项

### 1. Web API 写操作缺少跨站请求防护

`internal/server/server.go` 当前主要设置响应安全头，但写接口没有统一校验请求的
`Origin`、`Host`、`Content-Type` 或 CSRF token。本地服务包含多个会修改 Library、Source、
Activation 和 Project 状态的 POST/DELETE 接口，因此发布前应增加统一中间件，并为允许和拒绝
场景补测试。

验收标准：

- 仅接受预期的本机 Origin/Host；无 Origin 的 CLI 或受信任调用规则必须明确。
- 写接口仅接受规定的 JSON Content-Type，或使用不可被简单跨站表单构造的 CSRF 方案。
- 所有状态变更路由共用同一套防护，并覆盖正向、跨站和异常请求测试。

### 2. Web 更新 Git Source 后未同步已部署 Skill

`web/components/library.js` 的更新操作调用 `/api/sources/{name}/update`，该接口刷新 Library
内容；仓库另有 `/api/sync`，但当前 UI 更新流程没有调用它。因此 Source 更新成功后，已经启用
到用户 Agent 或项目中的副本仍可能停留在旧版本。

验收标准：

- UI 明确提供“仅更新来源”和“更新并同步部署”，或更新后自动执行可预览的同步计划。
- 同步失败时保留一致状态并显示可操作的错误信息。
- 自动化测试验证用户级 activation 和项目 copy/link 的更新结果。

### 3. 空白项目无法从 Web 首次部署

Web 项目的 Agent 选项来自扫描结果，而当前扫描逻辑只识别已经存在的
`.<agent>/skills` 目录。新项目尚未创建任何 Agent Skill 目录时，页面没有可选 Agent，因而无法
完成第一次部署。

验收标准：

- 空白项目也能选择已支持的 Agent，并在确认后创建目标目录。
- 首次 link、copy、取消部署和重复部署均有 API 与浏览器测试。
- 页面区分“项目尚未使用此 Agent”和“目录存在但状态异常”。

## P1：建议下一阶段完成

- 为 Project Require 增加干净 HOME、真实本地 Git remote、锁定 revision/hash 的恢复验收。
- 建立真实浏览器 E2E，至少覆盖 Library 导入、Source 更新、启用、项目部署和冲突提示。
- 处理 Linux UI 路径输入：当前原生目录选择只在 macOS 可用，不应让只读输入框阻断流程。
- 扩展 Doctor 到所有配置 Agent，并正确区分 followed-project 变化、fallback 和实际损坏。
- 在 UI/CLI 帮助中解释 Skill 的调用提示，例如 Codex 的 `$skill` 与部分工具的 `/command`，
  并提示新安装或变更后可能需要刷新会话。
- 新增 push/PR CI，执行 `go test ./...`、`go test -race ./...`、`go vet ./...`、gofmt 检查和安装测试。
- 对多目标部署和项目删除加入中途失败注入，验证回滚或可安全重试。

## P2：体验与维护完善

- 增加完整的 Source 管理页，统一呈现删除、更新、同步和依赖关系。
- 在 Web UI 补齐 Project Require、Vendor、Apply 工作流。
- 清理已经失效的 activation 映射和翻译状态。
- 移除仓库根目录中被跟踪的大体积 `skm` 二进制，改由构建或 Release 产物提供。
- 为 macOS Release 增加代码签名和 notarization。
- 评估设计文档中的后续能力：vendor diff、promote、反向依赖索引和离线镜像。

## 本次审查验证记录

以下命令已通过：

```bash
go test ./...
go test -race ./...
go vet ./...
go build ./...
sh scripts/dev_smoke_test.sh --race
git diff --check
```

测试覆盖率汇总约为 61.6%（2564/4165），其中 `internal/catalog` 约为 32.8%，应优先补足
Source、依赖关系和失败分支。另有两项环境或质量缺口：

- 当前环境没有可用的浏览器会话，因此尚未完成真实浏览器验收。
- 当前环境未安装 GoReleaser，因此尚未执行 `goreleaser check` 或 snapshot 构建。
- gofmt 检查发现 `internal/server/api_overview.go` 不是标准格式；本次仅整理文档，未修改业务代码。
